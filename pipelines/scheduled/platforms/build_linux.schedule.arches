# ROOTFS_IMAGE_NAME    TRIPLET                       ARCH           ARCH_ROOTFS    MAKE_FLAGS                                                                                         TIMEOUT       ROOTFS_TAG     ROOTFS_HASH
package_linux          x86_64-linux-gnusrcassert     x86_64         x86_64         USE_BINARYBUILDER=0,FORCE_ASSERTIONS=1,LLVM_ASSERTIONS=1,JL_CFLAGS=-Werror,JL_CXXFLAGS=-Werror     240           v8.8           7db0eac9cfbe5b38ecb05d2b91881f46cff4097d

# These special lines allow us to embed default values for the columns above.
# Any column without a default mapping here will simply substitute a `.` to the empty string

# From-source builds compile every dependency (incl. LLVM), so they get an
# explicit, much larger timeout than the BinaryBuilder-based builds.
#default TIMEOUT 80
