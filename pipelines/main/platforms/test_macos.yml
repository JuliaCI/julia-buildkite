steps:
  - group: "${GROUP?}"
    steps:
      - label: ":macos: test ${TRIPLET?}"
        key: "test_${TRIPLET?}"
        if: (build.env("BUILDKITE_STEP_KEY") == "test_x86_64-apple-darwin") || (build.branch == "master") || (build.branch == "main") # TODO: remove this line
        depends_on:
          - "build_${TRIPLET?}"
        plugins:
          - JuliaCI/external-buildkite#v1:
              version: "./.buildkite-external-version"
              repo_url: "https://github.com/JuliaCI/julia-buildkite"
        timeout_in_minutes: ${TIMEOUT?}
        soft_fail: ${ALLOW_FAIL?}
        commands: "bash .buildkite/utilities/test_julia.sh"
        agents:
          queue: "julia"
          os: "macos"
          arch: "${ARCH}"
        env:
          JULIA_SHELL: "/bin/bash"
          TRIPLET: "${TRIPLET?}"
