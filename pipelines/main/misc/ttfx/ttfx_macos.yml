steps:
  - group: "${GROUP?}"
    steps:
      - label: ":macos: TTFX ${TRIPLET?}"
        key: "ttfx_${TRIPLET?}"
        depends_on:
          - "build_${TRIPLET?}"
        # The job's own status rather than its group's, so a pull request the launcher
        # does not measure shows none
        notify:
          - github_commit_status:
              context: "TTFX"
        plugins:
          - JuliaCI/external-buildkite#v1:
              version: "./.buildkite-external-version"
              repo_url: "https://github.com/JuliaCI/julia-buildkite"
        commands: "bash .buildkite/utilities/ttfx/ttfx_ci.sh"
        timeout_in_minutes: ${TIMEOUT?}
        agents:
          queue: "test"
          os: "macos"
          arch: "${ARCH}"
        env:
          TRIPLET: "${TRIPLET?}"
